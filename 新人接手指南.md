# SQL 自适应学习平台 — 新人接手指南

> 本文档面向接手本项目的新同学，用最直白的语言说清楚：这个项目是干什么的、怎么跑起来、代码结构是怎样的。

---

## 一、一句话描述

**这是一个「SQL 刷题网站」，但和普通刷题网站不同——它会根据你的答题表现，自适应地推送最适合你当前水平的下一道题，实现"千人千面"。**

类比：就像是 Duolingo 的 SQL 版——你做对的题，系统知道这个知识点你已经掌握了，就不再重复；你做错的题，系统会继续给你同一知识点的题，直到真正掌握为止。

---

## 二、核心概念

### 2.1 三个角色

| 角色 | 说明 |
|------|------|
| **知识图谱** | 一张"知识点地图"，定义了26个 SQL 知识点之间的依赖关系（如学习 JOIN 前必须先掌握 SELECT） |
| **用户画像** | 系统对每个用户的知识掌握程度的"数字画像"，用 BKT（贝叶斯知识追踪）算法维护 |
| **自适应引擎** | 系统的"决策大脑"，读用户画像 → 算出最该推荐的下一题 |

### 2.2 三个阶段

| 阶段 | 做题量 | 策略 |
|------|--------|------|
| **冷启动期** | 0-5 题 | 摸底诊断（易/中/难各一题），快速评估初始水平 |
| **探索-利用期** | 6-30 题 | Thompson 采样 + BKT 更新，平衡"巩固已知"和"探索未知" |
| **精熟期** | >30 题 | 个性化排序 + 遗忘干预，精准补弱 |

### 2.3 答题流程

```
登录 → 摸底测试（3道诊断题）→ 自适应刷题主循环
                                   ├── 答对选择题 → 自动弹出同知识点填空题验证
                                   ├── 答对填空题 → 推荐下一知识点
                                   └── 答错 → 同知识点继续练习
```

---

## 三、技术栈（一句话版）

| 层面 | 用什么 | 备注 |
|------|--------|------|
| 后端 | Python + Flask | 轻量级，全部代码在 `backend/` 下 |
| 数据库 | SQLite | MVP 阶段用 SQLite，无需额外部署 |
| 前端 | 纯 HTML/CSS/JS | 零依赖，一个文件就是一个页面，无构建步骤 |
| 题库 | `questions.py` | 一个 2878 行的 JSON 大数组，每道题 12 个字段 |
| 部署 | `start_server.bat` + ngrok | 内网穿透用于公网访问 |

---

## 四、项目文件结构

```
SQL自适应训练/
├── 项目方案_AI参考手册.md          ← 【必读】项目蓝图：竞品、算法、里程碑
├── 自适应算法指引.md               ← 【必读】自适应引擎的设计文档
├── 填空题题目格式规范.md            ← 【必读】题库中每道题的字段规范
├── 新人接手指南.md                 ← 你现在在读的文件
│
├── questions.py                   ← 核心：全部题库（2878行JSON数组）
│
├── start_server.bat               ← 一键启动脚本（Windows）
│
├── backend/                       ← 后端代码
│   ├── run.py                     ← 入口文件，启动 Flask 服务
│   ├── app.py                     ← Flask 路由（所有 API 接口）
│   ├── database.py                ← 数据库操作 + 自适应引擎核心逻辑
│   ├── scraper.py                 ← 爬虫（尝试从 w3resource 爬题，但没成功）
│   ├── requirements.txt           ← Python 依赖
│   ├── questions.db               ← SQLite 数据库文件（运行时自动生成）
│   │
│   ├── trim_big_tables.py         ← 工具：缩减题目中过大的测试数据
│   ├── final_trim.py              ← 工具：最终版数据缩减
│   ├── fix_last_6.py              ← 工具：修复6道有问题的题目数据
│   ├── check_empty.py             ← 工具：检查哪些题缺少 expected_output
│   ├── check_after_trim.py        ← 工具：trim 后验证
│   ├── check_germany.py           ← 工具：调试某道德国相关题
│   ├── find_big_tables.py         ← 工具：找出数据量过大的题
│   └── debug_trim.py              ← 工具：调试数据缩减
│
├── frontend/                      ← 前端页面（纯HTML）
│   ├── login.html                 ← 登录页
│   ├── index.html                 ← 主刷题页（最核心的前端文件）
│   ├── admin.html                 ← 管理后台（看用户统计数据）
│   ├── login_ngrok.html           ← ngrok 版本的登录页
│   └── index_ngrok.html           ← ngrok 版本的主刷题页
│
├── docs/
│   └── 题目格式规范.md             ← 填空题格式规范（早期版本）
│
├── ngrok/
│   └── ngrok.exe                  ← ngrok 内网穿透工具
│
└── 文件/                          ← 调研阶段的产物
    ├── 调研初版.html
    ├── 调研全面版.html
    ├── 调研删减版.html
    ├── 调研极简版.html
    ├── 调研全面版.docx
    └── SQL知识图谱可视化.html
```

---

## 五、数据库结构（8 张表）

| 表名 | 存什么 | 重要性 |
|------|--------|--------|
| `questions` | 所有题目（题干、答案、选项、建表SQL、测试数据、预期输出） | ⭐⭐⭐ |
| `derived_questions` | 衍生题（由选择题自动生成的填空题，用于"举一反三"） | ⭐⭐ |
| `knowledge_nodes` | 知识图谱节点（26个SQL知识点） | ⭐⭐⭐ |
| `knowledge_edges` | 知识图谱的边（知识点间的前置依赖关系） | ⭐⭐⭐ |
| `question_knowledge` | 题目↔知识点的映射 | ⭐⭐ |
| `user_progress` | 用户答题记录（每题是否答对、耗时等） | ⭐⭐ |
| `user_mastery` | 用户对每个知识点的掌握度（BKT 的 α/β 值） | ⭐⭐⭐ |
| `journey_state` | 用户自适应旅程的当前状态（当前阶段、跳过节点等） | ⭐⭐ |
| `users` | 用户注册信息 | ⭐ |
| `diagnostic_results` | 摸底测试结果 | ⭐ |

---

## 六、API 接口速览

| 路由 | 方法 | 用途 |
|------|------|------|
| `/` `/index.html` | GET | 刷题主页 |
| `/login` | GET | 登录页 |
| `/admin` | GET | 管理后台 |
| `/api/login` | POST | 登录/注册 |
| `/api/diagnostic` | GET | 获取摸底测试题（易中难各1道） |
| `/api/diagnostic/complete` | POST | 提交摸底结果 |
| `/api/diagnostic/result` | GET | 查询摸底结果 |
| `/api/questions` | GET | 获取题目列表（支持按分类/难度筛选） |
| `/api/questions/<id>` | GET | 获取单题详情 |
| `/api/submit` | POST | 提交答案、判题 |
| `/api/graph` | GET | 获取知识图谱数据 |
| `/api/journey/start` | POST | 开始自适应旅程 |
| `/api/journey/next` | POST | 获取下一道题（自适应引擎核心接口） |
| `/api/journey/status` | POST | 查看当前旅程状态 |
| `/api/journey/toggle_deep` | POST | 开关深度模式 |
| `/api/practice/derive` | POST | 生成衍生填空题 |
| `/api/practice/recommend` | POST | 衍生题后的下一题推荐 |
| `/api/progress/<session_id>` | GET | 查看某用户的答题记录 |
| `/api/admin/stats` | GET | 后台统计数据 |
| `/api/admin/users` | GET | 后台用户列表 |

---

## 七、如何跑起来

### 前置条件
- Python 3.12（路径见 `start_server.bat`）
- 安装依赖：`pip install flask flask-cors requests beautifulsoup4 lxml`

### 启动步骤
```bash
# 方式一：一键启动
双击 start_server.bat

# 方式二：命令行启动
python backend/run.py
```

启动后访问：`http://localhost:5000`

### 如果想公网访问（给别人用）
把 ngrok 的域名配置到 `index_ngrok.html` 中，通过 ngrok 暴露 5000 端口。

---

## 八、自适应引擎核心逻辑（在 `database.py` 的 `journey_next` 函数里）

这是整个项目最核心的一段代码，理解它就行：

```
journey_next(session_id, 上道题答对了没, 花了多少秒)
    │
    ├── 阶段判断（冷启动→探索期→精熟期）
    │
    ├── 深度模式：如果上道是选择题且答对了 → 出同知识点填空题
    │
    ├── Thompson 采样选知识点：
    │   ├── 得分 = λ×探索分 + (1-λ)×利用分
    │   ├── 探索分 = 知识点的"不确定性"（越不确定越想探索）
    │   └── 利用分 = 知识点的"还没掌握"程度（越薄弱越想复习）
    │
    ├── 安全兜底：不连续5题同模块、不难度过高
    │
    └── 返回下一道题
```

BKT 更新逻辑很简单：答对 → `α+1`，答错 → `β+1`，掌握概率 = `α/(α+β)`。

---

## 九、题目格式

每道题是 12 个字段的 JSON 对象，详情见 `填空题题目格式规范.md`。核心字段：

| 字段 | 示例 | 说明 |
|------|------|------|
| `source` | `"curated"` | 来源 |
| `category` | `"where_andor"` | 知识点分类 |
| `difficulty` | `"easy"` | 难度 |
| `title` | `"AND 条件查询"` | 题目标题 |
| `description` | 题干文本 | 可含 `\n` 换行 |
| `table_schema` | `CREATE TABLE ...` | 建表语句 |
| `initial_data` | `INSERT INTO ...` | 测试数据 |
| `correct_answer` | `SELECT ...` | 标准答案 |
| `options` | `选项1\|选项2\|选项3\|选项4` | 有 `options` 的是选择题，没有的是填空题 |
| `option_explanations` | `✓正确\|✗错误1\|✗错误2\|✗错误3` | 选项解析 |
| `explanation` | 答案解析 | 纯文本 |
| `expected_output` | ASCII 表格 | 执行正确答案后的预期输出 |

---

## 十、当前项目状态

| 方面 | 状态 |
|------|------|
| MVP 后端 | ✅ 完成（Flask + SQLite） |
| 知识图谱 | ✅ 26个节点，有依赖边，已内置 |
| BKT 自适应引擎 | ✅ 三阶段策略完整实现 |
| 前端刷题页 | ✅ 支持选择题+填空题、自适应模式、手动选题模式 |
| 登录系统 | ✅ 简易用户名密码登录 |
| 管理后台 | ✅ 用户统计、答题数据图表 |
| 题库 | ✅ 约 2878 行 JSON，含大量爬取自 SQLZoo 的题 |
| LLM 生成变式题 | ❌ **未实现**（设计文档中有规划，但代码中只做了"选择题自动转填空题"） |
| IRT 模型 | ❌ **未实现**（设计文档中有规划，实际只用了 BKT） |
| ClickHouse/Neo4j | ❌ **未实现**（MVP 阶段用 SQLite 代替） |
| Redis 缓存 | ❌ **未实现**（MVP 阶段未引入） |
| 正式上线部署 | ❌ 当前是本地开发态 |

---

## 十一、待办事项 / 可改进方向

1. **LLM 集成**：设计文档中规划了"答错时用 LLM 生成同源变式题"，目前完全没做
2. **题量扩充**：当前题库以 SQLZoo 爬取题为主，缺少原创"组织场景"题
3. **前端优化**：index.html 约 2000+ 行，可以拆分成组件
4. **部署**：当前是 `python run.py` 裸跑，生产环境应加 gunicorn/nginx
5. **测试**：项目中没有测试代码
6. **ngrok 版本文件**：`index_ngrok.html` 和 `login_ngrok.html` 与普通版几乎一样，维护两套容易不同步

---

## 十二、开发建议

- **要改题库**：编辑 `questions.py`，然后删掉 `backend/questions.db` 重启服务即可重新播种
- **要改自适应逻辑**：主要改 `backend/database.py` 中的 `journey_next()` 函数
- **要改页面**：改 `frontend/index.html`（别改 ngrok 版，考虑合并）
- **要加 API**：在 `backend/app.py` 中加路由，逻辑放 `backend/database.py`

---

> 最后更新：2026-07-28
